<?xml version="1.0" encoding="utf-8"?>
<odoo>
<template id="report_invoice_pset_layouted" inherit_id="l10n_bg_extend.report_invoice_document">
    <xpath expr="//table[@name='invoice_line_table']" position="attributes">
        <attribute name="groups">!sale.group_sale_layout</attribute>
    </xpath>
    <xpath expr="//table[@name='invoice_line_table']" position="after">
        <t groups="sale.group_sale_layout" t-foreach="o.order_lines_sets_layouted()" t-as="page" name="lines_layouted">
            <table class="table table-condensed">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th class="hidden">Source Document</th>
                        <th class="text-right">Quantity</th>
                        <th class="text-right">Unit measure</th>
                        <th class="text-right">Unit Price</th>
                        <!-- <th class="text-right" groups="sale.group_discount_per_so_line">Disc.(%)</th> -->
                        <th class="text-right">Taxes</th>
                        <th class="text-right">Amount</th>
                    </tr>
                </thead>
                <tbody class="invoice_tbody">
                    <t t-foreach="page" t-as="layout_category">
                        <t t-if="layout_category_size > 1 or page_size > 1" groups="sale.group_sale_layout">
                            <tr class="active">
                                <td colspan="5" style="font-weight: bold; border-bottom: 1px solid black;">&amp;bull;
                                    <t t-esc="layout_category['name']"/>
                                </td>
                                <td class="text-right">
                                    <span t-esc="layout_category['quantity']"/>
                                </td>
                                <td class="text-right">
                                    <span t-esc="layout_category['price_unit']" t-options="{'widget': 'monetary', 'display_currency': o.currency_id}"/>
                                </td>
                            </tr>
                        </t>

                        <!-- Lines associated -->
                        <t t-foreach="layout_category['lines']" t-as="l">
                            <tr>
                                <td>
                                    <span t-if="l.discount==0.0">
                                        <span t-field="l.name"/>
                                    </span>
                                    <span t-if="l.discount &lt; 0.0">
                                        <span t-field="l.name"/>   (<span t-esc="l.discount * 100"/><span>%)</span>
                                    </span>
                                </td>
                                <td class="hidden"><span t-field="l.origin"/></td>
                                <td class="text-right">
                                    <span t-field="l.quantity"/>
                                </td>
                                <td class="text-right">
                                    <span t-field="l.uom_id" groups="product.group_uom"/>
                                </td>
                                <td class="text-right">
                                    <span t-field="l.price_unit"/>
                                </td>
                                <td class="text-right">
                                    <span t-esc="', '.join(map(lambda x: (x.description or x.name), l.invoice_line_tax_ids))"/>
                                </td>
                                <td class="text-right" id="subtotal">
                                    <span t-field="l.price_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": o.currency_id}'/>
                                </td>
                            </tr>
                        </t>

                        <t t-if="(layout_category_size > 1 or page_size > 1) and layout_category['subtotal']" groups="sale.group_sale_layout">
                            <t t-set="subtotal" t-value="sum(line.price_subtotal for line in layout_category['lines'])"/>
                            <t t-set="unit_price" t-value="subtotal/layout_category['quantity']"/>
                            <tr>
                                <td colspan="7" class="text-right">
                                    <strong>Subtotal: </strong>
                                    <span t-esc="subtotal" t-options="{'widget': 'monetary', 'display_currency': o.currency_id}"/>
                                </td>
                            </tr>
                        </t>
                    </t>
                </tbody>
            </table>

            <t t-if="page_index &lt; page_size - 1" groups="sale.group_sale_layout">
                <p style="page-break-before:always;"> </p>
            </t>
        </t>
    </xpath>
</template>
</odoo>
